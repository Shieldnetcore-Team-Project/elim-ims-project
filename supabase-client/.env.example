# Copy to .env.local and fill in from `supabase status` (local) or the
# project's API settings (hosted). Never commit the real .env.local.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
